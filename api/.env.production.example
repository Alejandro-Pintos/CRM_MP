# ============================================
# CONFIGURACIÓN DE PRODUCCIÓN - BACKEND API
# ============================================
#
# INSTRUCCIONES:
# 1. Copia este archivo como .env en producción
# 2. Reemplaza los valores de ejemplo con los de tu servidor
# 3. Ejecuta: php artisan key:generate
# 4. Ejecuta: php artisan jwt:secret
# 5. NO subas el archivo .env real a Git
#

# === APLICACIÓN ===
APP_NAME="E.R.P MADERAS PANI"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://api.maderaspani.com

# === BASE DE DATOS ===
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=crm_mp_production
DB_USERNAME=tu_usuario_db
DB_PASSWORD=tu_password_seguro

# === FRONTEND (CORS) ===
# URL del frontend para permitir peticiones CORS
FRONTEND_URL=https://maderaspani.com

# === JWT AUTHENTICATION ===
# Generar con: php artisan jwt:secret
JWT_SECRET=
JWT_TTL=60
JWT_REFRESH_TTL=20160

# === CACHE & SESSIONS ===
CACHE_STORE=database
SESSION_DRIVER=database
SESSION_LIFETIME=120

# === MAIL ===
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu-email@gmail.com
MAIL_PASSWORD=tu-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@maderaspani.com
MAIL_FROM_NAME="${APP_NAME}"

# === LOGS ===
LOG_CHANNEL=stack
LOG_LEVEL=error

# === CLIMA (OpenWeatherMap) ===
# Obtén tu API key en: https://openweathermap.org/api
OPENWEATHER_API_KEY=tu_api_key_aqui

# === OPCIONALES ===
BCRYPT_ROUNDS=12
QUEUE_CONNECTION=database
